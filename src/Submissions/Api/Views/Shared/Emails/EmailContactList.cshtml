@inject IOptions<SiteConfigOptions> _config
@inject IHostEnvironment HostEnvironment
@using Biobanks.Submissions.Api.Config
@using Biobanks.Submissions.Api.Models.Emails
@using Microsoft.Extensions.Hosting
@using Microsoft.Extensions.Options
@{
	var directoryName = @_config.Value.ServiceName;
	
	var emailSignature = @_config.Value.EmailSignature;

	// Format Email Subject
	ViewBag.Subject = HostEnvironment.IsDevelopment()
		? $"{directoryName} Contact List"
		: $"{directoryName} Contact List (Environment: {HostEnvironment.EnvironmentName})";
}

<p>Kindly find below the contact information for the Organisations in your list</p>

@Html.Raw(ViewBag.ContactList)

<p>If you didn't request this, please ignore this email.</p>

<p>Regards,</p>

@emailSignature
