@using System.Web.Optimization
@model Biobanks.Web.Models.Biobank.CopyCollectionModel
@{
    ViewBag.Title = "Copy collection";
}

@section FooterScripts
{
    @Scripts.Render("~/bundles/collection-and-capability-crud")
    @Scripts.Render("~/bundles/help-buttons")
}

<h2>@ViewBag.Title</h2>


@Html.Partial("_DataProtectionAlert")

@using (Html.BeginForm("CopyCollection", "Biobank", FormMethod.Post, new { @class = "form-horizontal col-sm-10" }))
{
    @Html.AntiForgeryToken()

    @Html.HiddenFor(x => x.OriginalId)

    @Html.Partial("_CollectionSharedFields", Model)

    <div class="form-group">
        <div class="col-sm-12 text-center">
            @Html.ActionLink("Cancel", "Collection", new { id = Model.OriginalId }, new { @class = "btn btn-default" })
            <button type="submit" class="btn btn-primary">Create collection</button>
        </div>
    </div>
}